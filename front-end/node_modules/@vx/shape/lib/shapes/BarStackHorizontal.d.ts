import React from 'react';
import { SeriesPoint } from 'd3-shape';
import { BarStackProps } from './BarStack';
export declare type BarStackHorizontalProps<Datum> = Pick<BarStackProps<Datum>, 'data' | 'className' | 'top' | 'left' | 'keys' | 'order' | 'offset' | 'value' | 'xScale' | 'yScale' | 'color' | 'children'> & {
    /** Returns the value mapped to the x0 of a bar. */
    x0?: (d: SeriesPoint<Datum>) => number;
    /** Returns the value mapped to the x1 of a bar. */
    x1?: (d: SeriesPoint<Datum>) => number;
    /** Returns the value mapped to the y of a bar. */
    y: (d: Datum) => number;
};
export default function BarStackHorizontal<Datum>({ data, className, top, left, y, x0, x1, xScale, yScale, color, keys, value, order, offset, children, ...restProps }: BarStackHorizontalProps<Datum> & Omit<React.SVGProps<SVGRectElement>, keyof BarStackHorizontalProps<Datum>>): JSX.Element;
//# sourceMappingURL=BarStackHorizontal.d.ts.map